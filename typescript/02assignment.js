/**
 * we can provide types in two ways
 *
 * 1. explicit
 * 2. implicit  (compilers guesses the type and this is also referred to as infer)
 *
 * @format
 */
// explicit
var variable = "message";
console.log(variable);
// implicit
var var1 = "message1";
console.log(var1);
